package Items;

public class RangedWeapon extends Weapon{
    //variabel för range stat
    private int rangedRange;

    //konstruktor för RangedWeapon-klassen
    public RangedWeapon(int weight, String name) {
        super(weight, name);
    }
}
